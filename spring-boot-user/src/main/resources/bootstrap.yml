spring:
  cloud:
    nacos:
      config:
        # 配置管理服务器地址
        server-addr: nacos:8080
        file-extension: yaml
  application:
    name: spring-boot-user
  profiles:
    active: dev

server:
  port: 8899
  tomcat:
    max-threads: 20