spring:
  cloud:
    nacos:
      config:
        # 配置管理服务器地址
        server-addr: nacos:8080
        file-extension: yaml
  application:
    name: spring-boot-gateway
  profiles:
    active: lc

server:
  port: 8040