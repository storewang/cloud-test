spring:
  cloud:
    nacos:
      config:
        # 配置管理服务器地址
        server-addr: 192.168.20.13:8848
        file-extension: yaml
  application:
    name: spring-boot-stream-produce
  profiles:
    active: dev

server:
  port: 9091